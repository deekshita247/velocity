# Photography replacement manifest

`porsche-study.jpg` is the existing local `images/gallery/spiral-1.jpg` automotive photograph, reused without a network dependency. It shows a road car, not a documented motorsport event.

The hero, six spinal exhibits, five accordion categories, engineering lenses and Race Sequence intentionally reuse this source with different framing. These are content placeholders, not six different shoots. Replace the sources in `components/racing/raceData.ts` with licensed local race, grid, pitlane, night-race and engineering-detail photographs. No circuit, driver or event provenance is asserted.
